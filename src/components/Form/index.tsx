import FormButton from "./FormButton";
import FormInputText from "./FormInput";
import FormRoot from "./FormRoot";

export  const Form ={
    Root:FormRoot,
    PrimaryButton:FormButton,
    PrimaryInput:FormInputText
}